import SectionHeader from "../components/UI/section/SectionHeader";

const ProductDetail = () => (
    <div id="product_detail">
        <div className="container">
            <SectionHeader text="Product name here" />
        </div>
    </div>
);

export default ProductDetail;
